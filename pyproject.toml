[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "wmp-lab"
version = "0.1.0"
description = "IsaacLab migration of Go2 WMP training tasks."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["wmp_lab*"]
